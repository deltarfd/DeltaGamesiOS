name: iOS CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [main, master]

jobs:
  lint-build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Select Xcode
        run: sudo xcode-select -switch /Applications/Xcode.app

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Install CocoaPods
        run: |
          gem install cocoapods --no-document
          pod install --repo-update

      - name: Run SwiftLint
        run: swiftlint lint --config .swiftlint.yml DeltaGames

      - name: Build App
        env:
          RAWG_API_KEY: ${{ secrets.RAWG_API_KEY }}
        run: |
          xcodebuild \
            -workspace DeltaGames.xcworkspace \
            -scheme DeltaGames \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            -derivedDataPath DerivedData \
            clean build

      - name: Run App Tests (includes SPM dependencies)
        env:
          RAWG_API_KEY: ${{ secrets.RAWG_API_KEY }}
        run: |
          xcodebuild \
            -workspace DeltaGames.xcworkspace \
            -scheme DeltaGames \
            -destination 'platform=iOS Simulator,name=iPhone 15' \
            -configuration Debug \
            -derivedDataPath DerivedData \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults.xcresult \
            -only-testing:DeltaGamesTests \
            test

      - name: Generate Coverage Report
        if: always()
        run: |
          mkdir -p coverage

          if [ -d "TestResults.xcresult" ]; then
            xcrun xccov view --report TestResults.xcresult > coverage/coverage.txt
            xcrun xccov view --report --json TestResults.xcresult > coverage/coverage.json

            python3 - <<'PY'
import json
from pathlib import Path

coverage_json_path = Path("coverage/coverage.json")
summary_path = Path("coverage/summary.md")

data = json.loads(coverage_json_path.read_text())
target = next((t for t in data.get("targets", []) if t.get("name") == "DeltaGames.app"), None)

lines = ["# Coverage Summary", ""]
if not target:
    lines.append("DeltaGames.app target not found in coverage report.")
else:
    overall = target.get("lineCoverage", 0.0) * 100
    files = target.get("files", [])
    low = sorted(files, key=lambda f: f.get("lineCoverage", 0.0))[:10]

    lines.append(f"Overall line coverage: {overall:.2f}%")
    lines.append("")
    lines.append("## Lowest Coverage Files (Top 10)")
    lines.append("")
    lines.append("| File | Coverage |")
    lines.append("| --- | ---: |")
    for f in low:
      name = f.get("name", "unknown")
      pct = f.get("lineCoverage", 0.0) * 100
      lines.append(f"| {name} | {pct:.2f}% |")

summary_path.write_text("\n".join(lines) + "\n")
print("\n".join(lines))
PY
          else
            echo "TestResults.xcresult not found. Skipping coverage extraction." | tee coverage/summary.md
          fi

      - name: Upload Coverage Artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: coverage-report
          path: |
            coverage/
            TestResults.xcresult
          retention-days: 30

      - name: Upload Build Artifacts
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: build-output
          path: DerivedData/
          retention-days: 30

      - name: Upload Build Logs
        if: failure()
        uses: actions/upload-artifact@v5
        with:
          name: build-logs
          path: /tmp/xcodebuild_logs/
          retention-days: 7
